<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="@dimen/dp5"
        tools:context=".MainActivity">

            <androidx.appcompat.widget.AppCompatAutoCompleteTextView
                android:id="@+id/idAutoCompleteSearchCity"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="@dimen/sp15"
                android:drawableEnd="@android:drawable/ic_search_category_default"
                android:hint="@string/enter_city_name"
                android:inputType="textCapWords" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/idRvSearchHistory"
            android:layout_width="match_parent"
            android:layout_below="@+id/idAutoCompleteSearchCity"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingStart="@dimen/card_side_margin"
            android:paddingEnd="@dimen/card_side_margin"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:context="com.google.samples.apps.sunflower.WeatherDashboardActivity"
            tools:listitem="@layout/list_item_city" />

    </RelativeLayout>

</layout>